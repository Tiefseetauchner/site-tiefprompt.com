# Copy this file to deployconfig.env and fill in your server details.
# deployconfig.env is gitignored and must not be committed.

SERVER_HOST=example.com
REMOTE_PATH=/var/www/tiefprompt.com

# Optional: omit if defined in ~/.ssh/config
# SERVER_USER=deploy
# SERVER_PORT=22
# IDENTITY=~/.ssh/id_ed25519
